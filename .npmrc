# Force pnpm usage
package-manager=pnpm@8.15.0
auto-install-peers=true
strict-peer-dependencies=false

# Skip Puppeteer Chromium download during install (downloads ~170-280MB and can stall)
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
