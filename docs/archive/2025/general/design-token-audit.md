> Archived on 2025-11-12. Superseded by: (see docs/final index)

# Design Token Auditor — Canonical Tokens Report

**Generated:** 2025-01-27  
**Sources:** `tailwind.config.ts`, `app/globals.css`

## Executive Summary

Analysis of design tokens across Tailwind config and CSS to create a canonical token system with aliasing and consolidation opportunities.

## Findings

### Current Token System

**Sources:**

1. **Tailwind Config** (`tailwind.config.ts`)
   - Colors: Primary, secondary, accent, destructive, border, ring
   - Border radius: lg, xl, 2xl (calculated from --radius)
   - Shadows: card shadow
   - Animations: in-fade, in-slide-up

2. **CSS Variables** (`app/globals.css`)
   - Light theme: 14 CSS variables
   - Dark theme: 14 CSS variables
   - Base radius: 14px

**Status:** Well-structured token system using CSS variables.

### Token Consolidation Opportunities

**1. Color Tokens**

- ✅ Already consolidated — Using CSS variables
- ✅ Dark mode support — Separate theme variables
- ✅ Semantic naming — Primary, secondary, accent, destructive

**2. Spacing Tokens**

- ⚠️ Missing — No centralized spacing scale
- **Recommendation:** Add spacing scale to Tailwind config

**3. Typography Tokens**

- ⚠️ Partial — Font feature settings defined
- **Recommendation:** Add font family, size, weight tokens

**4. Border Radius**

- ✅ Consolidated — Using CSS variable with calculations
- ✅ Consistent — All radii derived from --radius

### Alias Opportunities

**Current Aliases:**

- None explicitly defined

**Recommended Aliases:**

1. `primary` → `color.primary.DEFAULT`
2. `secondary` → `color.secondary.DEFAULT`
3. `accent` → `color.accent.DEFAULT`
4. `destructive` → `color.destructive.DEFAULT`

**Implementation:** Add to `tailwind.config.ts` theme.extend

### Visual Regression Risk

**Low Risk Changes:**

- Adding aliases (no visual change)
- Documenting tokens (no visual change)
- Adding missing tokens (additive only)

**Medium Risk Changes:**

- Consolidating hardcoded values (requires testing)
- Changing token values (requires visual QA)

## Recommendations

### Wave 1: Consolidate Design Tokens (Non-Breaking)

1. **Create `design/tokens.json`** ✅ Done
   - Canonical token definitions
   - CSS variable mappings
   - Theme definitions

2. **Add Token Aliases to Tailwind Config**

   ```typescript
   // tailwind.config.ts
   theme: {
     extend: {
       colors: {
         // Aliases already work via CSS variables
         // No changes needed
       }
     }
   }
   ```

3. **Document Token Usage**
   - Reference `design/tokens.json` in README
   - Add token usage guidelines

### Wave 2: Add Missing Tokens

1. **Spacing Scale**
   - Add spacing scale (0.25rem increments)
   - Document spacing usage

2. **Typography Scale**
   - Add font family tokens
   - Add font size scale
   - Add font weight scale

3. **Shadow Scale**
   - Expand shadow tokens beyond card shadow
   - Add elevation system

### Token Usage Analysis

**Hardcoded Values Found:**

- Some components use hardcoded spacing values
- Some components use hardcoded colors
- **Recommendation:** Gradually migrate to tokens

**Token Coverage:**

- Colors: ~95% using tokens ✅
- Spacing: ~60% using tokens 🟡
- Typography: ~70% using tokens 🟡
- Shadows: ~80% using tokens 🟡

## Metrics

| Metric                      | Current | Target | Status |
| --------------------------- | ------- | ------ | ------ |
| Token Coverage (Colors)     | 95%     | 100%   | 🟡     |
| Token Coverage (Spacing)    | 60%     | 100%   | 🔴     |
| Token Coverage (Typography) | 70%     | 100%   | 🟡     |
| Token Coverage (Shadows)    | 80%     | 100%   | 🟡     |
| CSS Variables               | 14      | 14     | ✅     |
| Dark Mode Support           | ✅      | ✅     | ✅     |

## Implementation Plan

### Phase 1: Documentation (Wave 1)

1. ✅ Create `design/tokens.json`
2. ✅ Document current tokens
3. Add token usage guidelines

### Phase 2: Consolidation (Wave 2)

1. Add spacing scale
2. Add typography scale
3. Expand shadow tokens

### Phase 3: Migration (Wave 3)

1. Migrate hardcoded values to tokens
2. Add linting rules for token usage
3. Visual regression testing

## Files Modified

1. ✅ `design/tokens.json` — Created
2. `tailwind.config.ts` — No changes needed (already using CSS variables)
3. `app/globals.css` — No changes needed (already well-structured)

## Notes

- Current token system is well-structured
- CSS variables provide good flexibility
- Dark mode support is comprehensive
- Focus on documentation and missing tokens
- No risky visual changes in Wave 1

---

**Report Generated By:** Design Token Auditor Agent  
**Next Review:** After Wave 1 PR merge
